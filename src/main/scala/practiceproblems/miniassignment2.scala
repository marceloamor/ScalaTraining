package com.scala.practiceproblems

object miniassignment2 extends App {

  //user input practice
 /* println("Type Your name: ")
  var name: String = scala.io.StdIn.readLine()
  println("Type Your age: ")
  var age: String = scala.io.StdIn.readLine()
  println(f"Hello $name , aged $age")
*/
  //string literals practice
  //print("{\n\"donut_name\":\"Vanilla Donut\",\n\"quantity_purchased\":\"10\",\n\"price\":2.5\n}")

  //name and favourite movie activity
  /*println("Type Your name: ")
  var name: String = scala.io.StdIn.readLine()
  println("Type Your favourite movie: ")
  var movie: String = scala.io.StdIn.readLine()
  println(f"Hello $name , whose favourite movie is $movie")*/

  //Donut math activity
  /*var donut_cost = 2.5
  var donut_quantity = 10
  var total_cost = donut_cost * donut_quantity
  println(f"The total cost of $donut_quantity donut(s) is $$ $total_cost%1.2f")*/

  //number of character instances in word
/*  println("Pick a word ")
  var word = scala.io.StdIn.readLine()
  println("pick a character")
  var char = scala.io.StdIn.readChar()
  var count = 0
  for (c <- word) {
    if (c == char) {
      count += 1
    }
  }
  println(word + " has the letter " + char + " " + count + " times")*/

  //word reversal
  /*var word: String = "Hello"
  var reverse: String = ""
  for(a<- word.length to 1 by -1){
    reverse = reverse + word.charAt(a-1)
  }
  println(reverse)*/


  //reverse a string with .reverse method
/*  var string_reverse = "hello world"
  println(string_reverse.reverse)*/




}
